class Tile {
    constructor (tID, x , y, tileWidth, tileHeight, color) {
        this.tID = tID;
        this.x = x;
        this.y = y;
        this.tileWidth = tileWidth;
        this.tileHeight = tileHeight;
        this.color = color;
    }
}
